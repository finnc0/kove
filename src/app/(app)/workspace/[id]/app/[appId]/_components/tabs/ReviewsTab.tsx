import type { ReportPageData } from "./types";

export function ReviewsTab({ data }: { data: ReportPageData }) {
  // Phase 3: rating distribution, reviewCount, communityQuotes[],
  //          positiveSignals (quotes), positiveCount/negativeCount
  return (
    <div className="space-y-4">
      <p className="text-xs text-zinc-600">Reviews — coming in Phase 3</p>
    </div>
  );
}
