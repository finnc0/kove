import type { ReportPageData } from "./types";

export function PainPointsTab({ data }: { data: ReportPageData }) {
  // Phase 3: full painPoints[] list (title, severity, reviewCount, quote)
  return (
    <div className="space-y-4">
      <p className="text-xs text-zinc-600">Pain Points — coming in Phase 3</p>
    </div>
  );
}
